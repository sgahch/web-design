# 《高级网页设计》考试要求和评分标准

## 一、题目
制作一个健康、积极、实用的符合社会主义核心价值观的自定主题的网站。

## 二、要求（蓝色文字为必须完成的要求，或与分数直接相关的内容）
1. 命名规范。根目录名和“7.上交要求”同名。全站文件及子目录须使用语义化小写英文（如 news 等）。
2. 全站结构。首页 `index.html` 放根目录，其余页面放 `views/`。自编组件脚本（即下面第 3 点）放 `components/`，第三方插件和库放 `public/vendor/`。大尺寸实景图与音频视频分别放 `public/images/` 和 `public/media/`；样式表、小图标及 Logo 分别放 `assets/styles/`、`assets/icons/` 和 `assets/images/`。
3. 界面排版：全站风格统一、美观合理，除首页外每个页面必须包含页眉(网站名、导航栏/搜索等)、内容区、页脚。HTML 须语义化且结构合理，CSS 样式高效无冗余(允许通过 CDN 引入 Tailwind CSS、UnoCSS 或 Animate.css 等第三方样式库)。
4. JS 功能与技术指标
    (1) 功能数量：全站需独立规划并使用原生 JS 实现 **5 个**服务于主题和内容、且具备实用价值的功能（分布于 5-6 个 HTML 页面中，每页至少体现 1 个）。
    (2) 技术覆盖：上述 5 个功能的底层代码必须综合（灵活搭配）涵盖以下 7 项技术指标（鼓励在一个复杂功能中融合多项技术，例如在一个购物车功能中同时使用 DOM 交互、数据计算与本地存储）：
    - 现代语法：深度运用 ES6+ 特性（必含模板字面量、箭头函数、解构赋值与高阶数组方法等）。
    - DOM 交互：使用 DOM 实现至少 1 次动态视图更新（如增删留言、Tab 切换、滚动显隐或图像懒加载、音视频播放控制切换等）。
    - 表单与状态联动：使用表单 DOM 完成 1 次数据合法性校验（需拦截非法提交）；以及 1 次前端数据计算（如购物车全选与总价动态计算、多条件筛选过滤等等）。
    - 浏览器原生功能：利用浏览器 Web API 完成至少 1 次交互（例如：localStorage 存储、倒计时、页面平滑滚动、打印或系统通知等）。
    - 异步请求：使用 Fetch API 或 Ajax 获取自定义或第三方 JSON 数据，并动态渲染至页面 1 次。
    - 生态集成：使用第三方库 ECharts 1 次，Swiper 1 次，需懂得其调用方式和配置项。
5. 脚本规范与模块化。全站须以外部 `.js` 文件为主（推荐 `type="module"` 或 `defer`、`async` 属性）。内部嵌入脚本限 1 次，严禁使用行内脚本（如 `onclick="..."`）。全局共性逻辑须提取为独立脚本，特定页面或独立功能需单独建文件隔离。
6. 代码掌控力与学术诚信。若某段核心逻辑由 AI 生成须加注声明（如 `// 此功能由 AI 辅助生成并调试`），答辩时若无法解释代码逻辑或 API 作用、无法应对假设性修改或代码遗留 AI 对话痕迹，视为未能体现代码掌控力，相关得分清零。若发现项目整体高度雷同，涉事所有学生一律按 0 分处理并上报。
7. 上交要求。压缩网站根目录（使用 .7z 格式，开启最大压缩率，总大小限 20MB 内）。压缩包严格命名为**软件工程 2*0*班(转)学号后两位姓名**（勿加空格）。打包前须进行资源瘦身：清理所有未使用的冗余代码与文件，图片可适当压缩，音频或视频最多保留 2 个且时长限 1 分钟内。

## 三、考试形式和评分标准

| 评分标准 | 分数(满分 100) |
| --- | --- |
| 界面美观、布局合理，网站结构(文件夹和文件)组织合理和命名规范 | 5 分 |
| 演示时语言表达流畅、逻辑清晰 | 15 分 |
| JS 代码的简洁度、难度、知识点广度。每人有效代码至少 200 行（不含注释，若代码组件化封装度高、逻辑复杂，不受行数限制。严禁为了凑行数编写冗余代码） | 30 分 |
| 每位同学回答 3 个技术问题的准确度 | 50 分 |